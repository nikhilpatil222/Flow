node ('worker_node1') {
  stage('Build4') { // Get code
    // get code from our Git repository
    echo  'Build from git'
  }
  stage('Compile4') { // Compile and do unit testing
 echo  'Compile from git'

   
  }
}
