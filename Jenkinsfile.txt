node ('worker_node1') {
  stage('Build2') { // Get code
    // get code from our Git repository
    echo  'Build from git'
  }
  stage('Compile2') { // Compile and do unit testing
 echo  'Compile from git'

   
  }
}
