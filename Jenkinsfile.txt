node ('worker_node1') {
  stage('Build1') { // Get code
    // get code from our Git repository
    echo  'Build from git'
  }
  stage('Compile1') { // Compile and do unit testing
 echo  'Compile from git'

   
  }
}
